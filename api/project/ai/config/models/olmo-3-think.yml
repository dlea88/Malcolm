# Olmo 3 7B Think — reasoning variant with chain-of-thought.
# Requires: ollama pull olmo-3:7b-think

name: olmo-3-think
description: Olmo 3 7B Think for reasoning tasks
provider: ollama

connection:
  url: ${OLLAMA_URL:-http://localhost:11434}
  timeout: 60

model: olmo-3:7b-think
max_tokens: 1024
structured_output: json
