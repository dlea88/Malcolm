# Olmo 3 7B Instruct — instruction-tuned variant.
# Requires: ollama pull olmo-3:7b-instruct

name: olmo-3-instruct
description: Olmo 3 7B Instruct for instruction-following tasks
provider: ollama

connection:
  url: ${OLLAMA_URL:-http://localhost:11434}
  timeout: ${OLLAMA_TIMEOUT:-120}

model: olmo-3:7b-instruct
max_tokens: 512
structured_output: json
